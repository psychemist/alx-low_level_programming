#include <stdio.h>
/**
 * main - Entry function
 * Description: Prints the name of the program
 * Return: 0 (Success)
 */
int main(int argc __attribute__ ((unused)), char *argv[])
{
	printf("%s", argv[0]);

	return 0;
}
